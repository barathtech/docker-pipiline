pipline {
   agent "java-docker-slave" {
    stages {
     stage ('install') {
      steps {
          sh '''apt install nginx'''
         }
       steps {
          sh'''nginx -v'''
         }
       }
     }
   }
 }
